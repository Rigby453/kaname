# Тест-сессия на устройстве — 2026-06-30 (Redmi 2311DRK48G, Android 16)

> Живой прогон debug-сборки (ветка `night/2026-06-25-...`) на реальном телефоне.
> Сборка: `app-debug.apk`, `--dart-define=API_BASE_URL=https://kaizen-backend-d5fr.onrender.com`.
> Статусы: `[ ]` todo · `[~]` в работе · `[x]` сделано · `💬` нужно решение пользователя · `🔍` исследовать.

## A. Баги — чиню без вопросов
1. `[~]` **Залп уведомлений при старте** (~10 шт, 2 типа). Регрессия D1: `rescheduleAllReminders` на старте планирует прошедшие сегодня времена, в exact-режиме они стреляют сразу. Фикс в работе (агент).
2. `[ ]` **Две иконки профиля** на «Сегодня» (слева сверху + справа у шестерёнки) — убрать дубль.
3. `[ ]` **Краш Плана** «Null check operator used on a null value» (в logcat не попал — ловится в экране; искать `!` по коду Plan).
4. `[ ]` **Инсайт в дневнике обрывается** (снова; в main была правка `e768449` — проверить, в ветке ли).
5. `[ ]` **setState после dispose** в `_KaiReviewRowState` (найдено в логе 12:23) — guard на mounted.
25. `[ ]` **«Написать в поддержку»**: показывает кривой адрес `support@kaizen@gmail.com` (двойной @) + должно открывать почту (`mailto:`), а не показывать текст.

## B. Дублирование / упрощение UX — чиню, подтвердить направление
6. `💬` **Внешний вид в шестерёнке «Сегодня»** дублирует Профиль; есть «назад», нет «Сохранить» → убрать шестерёнку с Today, оставить в Профиле, сохранение автоматическое (объяснить в UI).
23. `💬` **Тумблер «главная задача»** дублирует приоритет (главное/важное/обычное) → убрать тумблер, оставить приоритет.
22. `💬` **Таймпикер**: вернуть циферблат по умолчанию, ввод с клавиатуры — по тапу (сейчас наоборот).

## C. Ядро — утренний разбор и задачи
5d. `💬` **Утренний разбор**: показывать ПРЕВЬЮ расстановки (перенесённые + существующие), пользователь подтверждает; в этом превью можно переставлять задачи местами (drag).
21. `💬` **Как отмечать задачу выполненной** — сейчас тап открывает редактор/настройки, долгий тап ничего; «галочки» нет.
4c. `🔍` **«AI couldn't build right now, tap retry»** — разбор не собрался (холодный старт Render? ошибка ИИ?).
31. `💬` **Иконки на карточках задач путают.** ⏰ (alarm) = тип дедлайн/экзамен, 📅 = событие, обычная = без иконки, у главных скрыто (`today_screen.dart:1081`). Это ТИП, а не напоминание. Индикатора «напоминание включено» нет вообще. → Добавить колокольчик при включённом напоминании; дедлайну дать менее «будильниковую» иконку.

## D. Сегодня — таймлайн
20. `💬` **Показывать весь день**, а не только занятое время — чтобы видеть, где «сейчас» относительно всех часов (сейчас задачи 12:00 и 22:00, «сейчас» 12:35 висит непонятно где).

## E. Kai — оживить
11. `💬` **Где вообще Kai и почему безликий.** Хочу: двигается, иногда меняет форму, реагирует — не статичная картинка. (См. `docs/MASCOT.md`, ADR-032, Rive.)

## F. Вода
12. `💬` **Раскрытие стакана**: по тапу рядом со стаканом (не на плашки), кнопку убрать.
13. `💬` **Отчёты воды**: не только столбцы, но и ломаная кривая; вид «месяц», а не 1 день.

## G. Дневник
8. `💬` **Очищать введённый текст после сохранения дня?** (пользователь сам не уверен — обсудить).
10. `🔍` **«Эта неделя» — ИИ-итоги работают не каждый раз** (нестабильно).

## H. Медитация / дыхание / фото-еда
15. `💬` **Озвучка медитации**: голос слишком быстрый/неприятный; фоновый звук как «нерабочие наушники», а не звуки природы.
16. `💬` **Объединить дыхание с медитацией** (связанные вещи).
14. `🔍` **ИИ-фото еды**: после съёмки долгое ожидание перед подтверждением фото.

## I. Здоровье — модули
17. `💬` **Почему вода/сон открыты полностью, а остальные модули «прикрыты»** (mode-flags, off by default) — объяснить логику, решить.

## J. Перетаскивание блоков
19. `💬` **На телефоне нельзя двигать/менять размер с первого касания** — надо сначала «выбрать» блок. Не нравится; хочу прямое взаимодействие.

## K. Стрик / заморозки / прогресс
24. `💬` **Заморозки = 0 при дев-премиуме.** Логика (`freeze_accrual_service.dart`): первый прогон только ставит часы (0 выдаёт), Premium +1/14 дней, +2 стартовых — ТОЛЬКО при покупке через paywall (`grantPurchaseBonus`). Дев-премиум не проходит paywall → бонуса нет. Работает как закодировано, но UX плохой: новый премиум сидит с 0 заморозок 14 дней. Решить: давать стартовые заморозки сразу.
28. `💬` **«Прогресс»**: история стрика — когда оборвался, когда держался и т.д.

## L. Профиль — структура / рефералы / поддержка / правовое
27. `💬` **Приглашение друга не отслеживается** — кнопка показывает «coming soon» (`profile.referral_coming_soon`). Нет реф-кода, нет бэкенда, нет атрибуции. Если нужно — проектировать с нуля.
26. `[ ]` **Privacy/Terms сырые** — полностью переписать в конце (поздняя задача).
29. `💬` **Дублирующий раздел «Аккаунт»** после имени: снова «Написать в поддержку» + «Условия/Конфиденциальность» (повтор). Выйти уже есть снизу → убрать раздел «Аккаунт», один «Выйти» в конце настроек.
30. `💬` **Нельзя поменять имя и аватар профиля** — добавить редактирование (имя + картинка/иконка профиля).

## M. Иконка приложения
7. `[ ]` **Кастомная иконка приложения не применилась** — стоит дефолтная. Нужен путь к файлу (на рабочем столе) → применить через flutter_launcher_icons.

---

## ✅ Прогресс — автономная сессия 2026-06-30 (батч уверенных фиксов)

Сделано (каждый агент: `analyze` 0; тесты дописаны, гоняю общим сьютом перед коммитом):

- **#1 уведомления** — переисследовано: время планирования везде корректно (будущие моменты), залп НЕ от прошедших времён. Реальная причина — boot-receiver реплеил кэш при `MY_PACKAGE_REPLACED` (переустановка) → просроченные сегодня слоты стреляли сразу. Убран `MY_PACKAGE_REPLACED` из receiver (ребут-выживание через `BOOT_COMPLETED` осталось). Плюс рефактор `nextInstanceAfterNow` + 19 тестов. **Нужна перепроверка на устройстве.**
- **#3 + #5 краш** — корень: `_KaiReviewCard.onDismiss()` (setState) после `await moveAllToDay` на disposed-строке → ErrorWidget → пять `theme.extension<FocusThemeExtension>()!` падали. Фикс: `context.mounted` guard + null-guard вместо `!` (5 мест).
- **#4 инсайт дневника** — клиппинг в `AlertDialog` (нет скролла) → обёрнут в `SingleChildScrollView`.
- **#12 вода** — тап по стакану раскрывает пресеты (GestureDetector opaque), плашки не сворачивают.
- **#22 таймпикер** — `initialEntryMode: dial`.
- **#23 тумблер «главная задача»** — убран; `isProtected = priority=='main'` сохранено (движок не сломан).
- **#24 заморозки** — стартовый грант Free 1 / Premium 3, идемпотентно (prefs-флаг).
- **#25 поддержка** — mailto `support.kaname@gmail.com` + fallback в буфер; кривой адрес исправлен.
- **#29 дубль «Аккаунт»** — убран; один «Выйти» снизу.
- **#2 две иконки профиля** — правая дубль-аватарка убрана.
- **#6 шестерёнка на Today** — убрана (внешний вид остаётся в Профиле).
- **#21 «выполнено»** — свайпы уже работали (право=готово/лево=пропуск) + добавлен тап по кружку (pending → markDone).
- **#31 индикатор** — колокольчик при `reminderMinutesBefore != null`; дедлайн `alarm`→`flag`, экзамен `graduationCap`.

Дальше по плану: общий `analyze`+тест → коммит в ветку → пересборка APK → переустановка. **Merge в main — после проверки на устройстве.** Отложенные `💬`-пункты (утренний разбор-превью, живой Kai, графики воды, звук медитации, дыхание+медитация, модули, драг, весь таймлайн, рефералы, история стрика, имя/аватар, Terms) — обсудить отдельно.
