# Тест-сессия 2026-06-30 — батч 2 (фидбек пользователя, телефон, ветка night)

> Второй поток фидбека после первого батча фиксов (5ad1242). Сборка на Redmi с боевым
> бэкендом `kaizen-backend-d5fr`. Статусы: `[ ]` todo · `💬` решение · `🔍` исследовать · `[~]` в работе.

## Онбординг
1. `💬+[ ]` **Структура онбординга путаная:** 3 слайда ДО входа в аккаунт → вход → ЕЩЁ онбординг.
   Непонятно зачем разбито. Пересмотреть поток (зачем слайды до и после входа).
2. `[ ]` **Слайд «добавь задачу» / демо переноса неадекватен:** просит добавить ОДНУ задачу и
   переносит её; нет времени, нет других задач, перенос абстрактный. Нужно нагляднее: по дефолту
   уже стоят несколько задач (с временем) + пользователь добавляет свою; одна/несколько «не
   успевают» выполниться; перенос показывается **настоящими кнопками/UI приложения**, а не
   абстрактной анимацией. Файл: `app/lib/features/onboarding/setup_flow.dart` (RescheduleDemo).
3. `[ ]` **«Откуда узнали о нас» — вместо «Другое» поле ввода:** либо сразу плашка с текстовым
   полем, либо «Другое» → раскрывается поле, куда человек пишет источник. (C1 онбординг.)
4. `💬` **Слайд пейвола — описать возможности ИИ лучше.** Предложить варианты копирайта (что даёт
   ИИ: умный перенос, меню, тренировки, инсайты). См. ниже предложенные тексты.

## Темы / контраст
5. `💬+[ ]` **«4 темы, а по факту чёрная и белая»:** темы не воспринимаются как 4 разных —
   читается как только тёмная/светлая. Разобраться, почему Focus/Calm/др. не отличаются визуально,
   усилить различие ИЛИ честно сократить.
6. `💬` **Высокий контраст — зачем?** Пользователь считает, что он просто увеличивает размер текста,
   а для размера текста уже есть отдельная функция. Решить: оставить/убрать/переосмыслить Contrast-тему.

## Screen-time (экранное время)
7. `[ ]` **Без лимита текст ломается:** «Использовано сего…ю» (обрезается), хотя место есть. С
   установленным лимитом — пишется нормально. Overflow/layout в карточке приложения без лимита.
   Файлы: `app/lib/features/health/screen_time*.dart`. Анти-регресс gate B.
8. `🔍` **«Всего сегодня 11ч 22м» — неверное общее время.** По отдельным приложениям считает верно,
   а суммарное — неправильно (завышено?). Проверить агрегацию total screen-time.

## Профиль
9. `[ ]` **Раздел «Аккаунт» в профиле всё ещё есть** (хотя в прошлой сессии помечен убранным #29) —
   там теперь только имя, которое и так в профиле → удалить раздел. ПРОВЕРИТЬ, почему фикс не виден.
10. `[ ]` **«Поделиться стриком» переместить** ближе к «Прогрессу»; ниже оставить только «поделились
    со мной» и «поделиться неделей».
11. `💬` **Премиум-профиль улучшить:** если у пользователя премиум — как-то выделять профиль (бейдж,
    особое имя/оформление). Связать с задачей имя/аватар (в работе).

## Виджет
12. `[ ]` **Android-виджет «не удалось загрузить виджет»** (A1) — до сих пор. Чинить + редизайн
    (уже описывал ранее, см. WIDGET.md / docs/STATUS A1-A5).

## Правовое / документы / синк
13. `[ ]` **Terms & Privacy — составить грамотно** + проверить, нужны ли ещё документы (оферта,
    согласие на обработку ПД, возрастные). Обновить также на **лендинге и в вебе**, без проблем с
    языком (мультиязычность).
14. `[ ]` **БАГ синка стрика:** зашёл в старый существующий аккаунт — задачи синхронизировались, а
    **стрик НЕТ** (как будто новый пользователь). Плюс **дали заморозку, хотя на телефоне заморозок
    нет** (заморозки ввели позже создания аккаунта). Проверить: синк стрика при login + логику
    стартового гранта заморозок (не должен перетирать/выдавать на старом аккаунте). HIGH.

## План / перетаскивание
15. `[ ]+💬` **Перетаскивание блоков — 1 касание + быстрее:** ВСЁ ещё работает только со 2-го касания
    (сначала «выбрать», потом тащить/менять размер). Курсор уже показывает resize (I) при наведении
    на низ блока, но размер меняется только со 2-го касания. Хочу как в Google Calendar — с 1 касания.
    Уменьшить и время удержания перед перетаскиванием. Файл: `app/lib/features/plan/widgets/time_grid.dart`.
16. `💬` **Непонятные задачи сверху таймлайна** (идея пользователя, связана с «размытыми задачами»):
    в других планерах задачи без времени висят СВЕРХУ (напр. 3 задачи), под ними — временные;
    можно перетащить наверх-вниз и дать время позже. Пользователю нравится, но не уверен. → заложить
    как один из вариантов UX для модели «форм» задачи (fuzzy-tasks).

## Здоровье / ИИ
17. `[ ]` **Отключённые модули должны ПРОПАДАТЬ, а не показывать плашку «включить»:** отключил
    медитацию и др. в настройках — в Здоровье они остались карточками с тумблером включения. Должны
    исчезать из Здоровья и включаться ТОЛЬКО в настройках. Файлы: `feature_modes_provider` + `health_screen`.
18. `🔍` **«Собрать меню с ИИ» не работает:** "AI is temporarily unavailable (quota/region)".
    Вероятно квота Gemini free-tier исчерпана ИЛИ гео. Проверить backend AI-провайдер, квоты, ретраи,
    понятную ошибку. Core платная фича — важно.

## Уведомления
19. `🔍` **Залп уведомлений про воду:** пока приложение было запущено — норм (за 3 мин прислало 1).
    Но «когда гулял» прислало **4 уведомления про воду в одно время** (возможно не было интернета).
    Связано с notif-burst — перепроверить логику воды-напоминаний (дедуп, оффлайн).

## Инфо (не баг)
20. **ЮKassa подписана — должна работать; RuStore пока нет.** (Контекст для биллинга.)

## Предложенные тексты пейвола (п.4) — на выбор
- **A (выгода):** «Главное с ИИ: умный перенос задач, когда день не сложился; меню и тренировки под
  твои цели; еженедельные инсайты — что реально мешает планам.»
- **B (коротко):** «ИИ-перенос дел · ИИ-меню и тренировки · разбор недели. Бесплатно — весь планер
  без ИИ.»
- **C (по пунктам):** «✓ Умный перенос несделанного ✓ Меню по КБЖУ ✓ Программа тренировок
  ✓ Инсайты дневника и недели».
