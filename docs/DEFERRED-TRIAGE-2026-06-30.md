# Разбор отложенных задач — верификация по коду (2026-06-30)

> Сгенерировано воркфлоу `verify-deferred-backlog`: 72 пунктов сверены с реальным кодом.
> Один агент (виджеты) отвалился на связи — Android-виджет A1 и lock-screen виджеты добрать отдельно.

## 🤔 Нужно решение пользователя (17)

- **[HIGH/L] core-fuzzy-tasks** — Модель «форм» задачи (конкретная/с дедлайном/размытая) для важных задач без времени
  - _код:_ app/lib/core/database/database.dart:44 — scheduledAt = dateTime()() НЕ nullable: каждая задача обязана иметь время, понятия «размытой/без времени» в модели нет. Grep по shape/fuzzy/untimed/clarify в app/lib/features/plan и add_task_sheet.dart — совпадений по сути модели нет (matches были на 'shape:' BoxDecoration и т.п.). Goals существуют (plan/goals_screen.dart, GoalsTable database.dart:315 + goal_steps), но связки «размытая задача → уточнить/таймбокс/в Цель» нет.
  - _рек:_ Это центральный концептуальный вопрос, не закодить в лоб — нужен дизайн + продуктовое решение. Рекомендую: (1) сделать scheduledAt nullable + ввести enum формы (concrete/deadline/fuzzy); (2) для fuzzy — флоу уточнения: предложить грубый тайм-бокс ИЛИ «когда сделать» ИЛИ разбить на шаги ИЛИ перевести в существующие Goals (инфраструктура goal_steps уже есть). Это меняет общий контракт (data-model + api-spec) — согласовать с пользователем до реализации.
- **[HIGH/L] morning-review-preview** — Утренний разбор: ПРЕВЬЮ расстановки задач с подтверждением и drag-перестановкой
  - _код:_ _KaiReviewCard в today_screen двигает задачи через moveAllToDay без экрана-превью; единственный ReorderableListView — для подзадач в add_task_sheet.dart:2548, не для разбора
  - _рек:_ Это ядро продукта (hook). Сделать: после разбора показывать предлагаемую раскладку (перенесённые + существующие) одним превью-листом с drag-reorder и кнопкой Подтвердить. Отдельно от prompt-логики /ai/redistribute (ai-morning-redistribute) — здесь именно UX подтверждения. Высокий приоритет.
- **[MEDIUM/S] D1-2** — В манифесте есть SCHEDULE_EXACT_ALARM, но НЕТ USE_EXACT_ALARM
  - _код:_ AndroidManifest.xml:9 объявлен android.permission.SCHEDULE_EXACT_ALARM; :13 RECEIVE_BOOT_COMPLETED; :4 POST_NOTIFICATIONS. USE_EXACT_ALARM в файле отсутствует
  - _рек:_ Для планировщика правильнее именно SCHEDULE_EXACT_ALARM — это и сделано. USE_EXACT_ALARM НЕ добавлять: Google Play разрешает его только приложениям, где будильник/таймер — основная функция; иначе риск отклонения при модерации. Оставить как есть, зафиксировать решение.
- **[MEDIUM/M] auth-pwreset-phone** — Нет восстановления пароля для аккаунтов по телефону (forgot принимает только email)
  - _код:_ backend/src/routes/auth-reset.ts:24 — forgotSchema = z.object({ email }); оба эндпоинта forgot/reset работают только по email (строки 33-129). Регистрация по телефону разрешена (auth.ts:17-32, 116-126), но reset для них недоступен. Клиент тоже шлёт только email: app/lib/features/auth/forgot_password_screen.dart:51 forgotPassword(email).
  - _рек:_ Вариант A (рекомендую): сделать email единым каналом сброса — при регистрации по телефону мягко просить/предлагать почту как контакт для восстановления; тогда телефонный юзер тоже восстановится через email. Это дешевле SMS (которое платное, шлюз) и сходится с пунктом про email-провайдер выше — делать одним блоком. Решать после подключения email. SMS-OTP не вводить (уже решено).
- **[MEDIUM/M] plan-4** — Несогласованный разворот мини-календаря между режимами
  - _код:_ plan_screen.dart:510 (Day) и :487 (Week, при !isGrid && !searchVisible) показывают ExpandableWeekCalendar; в threeDay (459-471), month (457), year (455) его НЕТ. _showsWeekStrip ограничен day+week (:682-683). Поведение разворота даты действительно разное между режимами — баг подтверждён.
  - _рек:_ Решение пользователя (нельзя просто закодить — это UX-выбор). Рекомендую вариант A из STATUS:818 — вынести мини-календарь-пикер отдельно от переключателя промежутка и сделать его доступным одинаково во всех режимах (переключатель = гранулярность ленты, календарь = выбор даты). Это устраняет рассогласование без потери функций и согласуется с фидбеком «слишком много кнопок» меньше, чем вариант C (убрать календарь).
- **[MEDIUM/L] food-6** — Food-DB смешанный язык — фильтр/локализация выдачи по языку пользователя
  - _код:_ backend/src/routes/food.ts и food/openFoodFacts.ts — ни одной ссылки на lang/locale/Accept-Language/lc (grep пуст); выдача Open Food Facts идёт без языкового параметра, отсюда смешение языков
  - _рек:_ Нужно продуктовое решение: (A) дёшево — пробрасывать Accept-Language клиента в OFF как lc=<lang> + сортировать по совпадению языка имени (быстрый buildable-кусок, рекомендую как первый шаг); (B) дорого — свой мультиязычный food-DB. Рекомендую A сейчас, B отложить. Развязать с распознаванием (dish name на языке юзера).
- **[MEDIUM/M] biz-pricing** — Пересмотр модели подписки/цены ($10 в SPEC vs 399₽-заглушка)
  - _код:_ Рассинхрон: SPEC/CLAUDE.md = $10/мес (ADR-052, freemium без рекламы), docs/pricing-model-todo.md = временные 399₽/мес заглушка с пометкой «надо просчитать юнит-экономику». STATUS.md:634-638 — пункт открыт, помечен «не код, а стратегия → ADR».
  - _рек:_ Чистое продуктовое решение, кодом не закрыть. Рекомендую зафиксировать ADR: РФ ~399₽/мес (или ниже по эластичности) + зарубеж $8-10 через гибридный биллинг; месяц+год со скидкой + короткий триал; в платном — ИИ-перенос (главный hook) + ИИ-меню/тренировки; бесплатный tier полностью рабочий без ИИ. Свести SPEC и pricing-model-todo к одному числу.
- **[MEDIUM/M] meditation-audio** — Озвучка медитации: голос быстрый/неприятный, фон не похож на природу
  - _код:_ meditation_audio.dart:21/85 — озвучка через системный flutter_tts + коричневый шум фоном; качество ограничено системным TTS
  - _рек:_ Decision: либо снизить speech rate и подобрать голос (быстро, дёшево), либо заменить на записанные аудио-ассеты природы + профессиональный голос (нужны файлы/CDN — внешняя зависимость). Рекомендую сначала подкрутить rate/паузы TTS как дешёвый шаг, аудио-ассеты — позже.
- **[MEDIUM/M] drag-direct-touch** — Перетаскивание блоков плана с первого касания (сейчас нужно сначала 'выбрать')
  - _код:_ Жалоба теста #19 (J): на телефоне нельзя двигать/ресайзить блок с первого тача — требуется предварительный выбор
  - _рек:_ Decision по UX: прямой long-press-drag vs текущий select-then-drag. На мобиле обычно long-press→drag сразу. Рекомендую убрать промежуточный 'выбор' и включить immediate long-press drag (баланс со случайными касаниями при скролле).
- **[MEDIUM/M] terms-privacy-content** — Условия использования + конфиденциальность — переписать содержание
  - _код:_ terms_screen.dart существует (экран есть), но содержание сырое/шаблонное; нужно описать сбор данных, локальное хранение/синк, ИИ-данные, возраст, согласие
  - _рек:_ Перед публичным релизом/модерацией кассы — переписать под реальные потоки данных (синк Neon, ИИ через Render, ЮKassa). Желательно сверка с юристом под орг-форму. Связано с yukassa-site-moderation.
- **[LOW/M] W4-volume** — Недельный объём по мышцам (подходов на группу в неделю)
  - _код:_ ai_workout_sheet.dart — нет поля объёма; workoutBuild.ts:188-217 промпт не задаёт целевой недельный объём по группам
  - _рек:_ Это decision: для студентов-новичков (целевая аудитория) выбор «подходов на группу в неделю» — переусложнение. Рекомендую НЕ давать числовой ввод, а оставить модели подбор объёма по experience; максимум — добавить позже пресет «низкий/средний/высокий объём». Не делать сейчас.
- **[LOW/S] auth-foreign-oauth** — Иностранный OAuth (Google/Apple) — открытый вопрос 406-ФЗ, в коде отсутствует
  - _код:_ Подтверждено: иностранного OAuth в коде НЕТ. app/lib/features/auth/auth_screen.dart:4 — комментарий 'Google/Apple OAuth удалены — запрещены 406-ФЗ'. Grep google/apple/oauth/signInWith по app/lib/features/auth дал только этот комментарий. Бэкенд: только email/phone+пароль (bcrypt+JWT, auth.ts).
  - _рек:_ Оставить как есть — НЕ добавлять (текущее решение пользователя верное и compliant). Тема не код, а юридическое решение: для РФ-владельца иностранный OAuth — спорная зона 406-ФЗ. Держать открытым вопросом до консультации юриста под орг-форму; возможный будущий сценарий — отдельная зарубежная сборка, где закон не действует. Никаких изменений в коде сейчас не требуется.
- **[LOW/S] today-6-completion-sound** — Звук завершения задачи — поменять
  - _код:_ Система звука есть и подключена: completion_sound_service.dart (играет assets/sounds/task_done.wav, fallback SystemSound.click), хук в items_dao.dart:210,474,544 (markDone/materializeOccurrence), тумблер в profile_screen.dart:1321-1335. Ассет app/assets/sounds/task_done.wav СУЩЕСТВУЕТ (синт. колокол E6→B6, ~0.6с, см. комментарий completion_sound_service.dart:35-37). README.md устарел (всё ещё пишет 'task_done.mp3 — TODO').
  - _рек:_ Инфраструктура и новый звук-замена уже на месте (вместо системного клика). Остаётся вкусовое решение пользователя: послушать текущий 'дзинь' и либо принять, либо положить свой файл в assets/sounds/task_done.wav (код подхватит без изменений). Заодно обновить устаревший assets/sounds/README.md (.mp3→.wav). Рекомендация: принять текущий звук, если устроит — это превращает пункт в done без кода.
- **[LOW/S] 2** — Дневник: состояние после сохранения / редактирование записи без дублей
  - _код:_ app/lib/features/diary/diary_screen.dart:61 _loadExisting() в initState читает запись за сегодня и заполняет форму (_mood, _parseNote) → форма НЕ пустая при повторном открытии; :102 dao.saveForDate — upsert (один ряд на день); :111-124 mood_logs first-save-wins (нет дублей); :130 после сохранения только SnackBar 'diary.day_saved'
  - _рек:_ Главные жалобы (пустая форма при повторном входе + дубли) уже устранены: форма подтягивает запись за сегодня и сохраняет через upsert. Открытым остаётся лишь косметика — нет явного режима «уже записано сегодня: … [Изменить]», после save форма просто остаётся редактируемой. Пользователь сам «не уверен, чего хочет» → решение продуктовое. Рекоменд.: оставить как есть либо добавить лёгкий бейдж «сохранено сегодня» у кнопки — низкий приоритет. Редактирование ПРОШЛЫХ дат (не только сегодня) пока невозможно (форма всегда грузит DateTime.now()) — связать с п.1.
- **[LOW/S] health-grouping** — Health — 4 группы (Питание/Осознанность/Движение/Сон) + убрать дефолтные вода+сон
  - _код:_ ГРУППИРОВКА СДЕЛАНА: health_screen.dart 4 секции — section_nutrition (Вода card стр.228 + Food tile стр.230), section_sleep (стр.240, _SleepCard), section_mind (Медитация стр.245 + Дыхание стр.254), section_movement (Workouts стр.265). Но «убрать дефолтные вода+сон» НЕ сделано: Вода (стр.228) и Сон (стр.241) по-прежнему всегда видимы, не за тумблерами/онбордингом. Движение содержит только Workouts — Разминка/Осанка отдельными тайлами в хабе не выведены (Posture = reminder-only по app/CLAUDE.md).
  - _рек:_ Основное (4 группы) — done, совпадает с памятью «сделано 2026-06-27». Остаток (сделать воду/сон управляемыми онбордингом, без магических дефолтов) — продуктовое решение: лично рекомендую ОСТАВИТЬ воду и сон всегда-видимыми (это базовые health-метрики, скрывать за тумблером ухудшит UX), а пункт закрыть как «решили не убирать». Если всё же убирать — это малый код в feature_modes_provider + health_screen.
- **[LOW/L] referral-tracking** — Рефералы: сейчас заглушка 'coming soon', нет кода/бэкенда/атрибуции
  - _код:_ profile_screen.dart:366 — onTap показывает snackbar profile.referral_coming_soon; реф-кода и бэкенда нет
  - _рек:_ Decision: нужна ли реф-программа сейчас. Если да — проектировать с нуля (реф-код, deep link, атрибуция при регистрации, награда). Большая фича; рекомендую отложить до запуска монетизации, оставить честный 'coming soon'.
- **[LOW/S] diary-save-affordance** — Дневник: неочевидное состояние после сохранения дня
  - _код:_ diary_screen.dart:61-66 _loadExisting() уже подтягивает запись дня (prefill mood/note), дублей нет; не хватает явного UI 'сегодня записано: … [изменить]'
  - _рек:_ Данные уже не теряются. Decision (пользователь сам не уверен): показать явный режим 'запись за сегодня сохранена' с кнопкой Изменить вместо неясного состояния. Мелкая правка UX.

## 🔨 Можно делать кодом (28)

- **[HIGH/S] W5-rest-bug** — БАГ: ИИ игнорирует пользовательский rest из настроек — не передаётся в промпт
  - _код:_ ai_workout_sheet.dart:145-155 _buildAi НЕ читает restDefaultProvider и не шлёт rest; api_client.dart:598-620 aiWorkoutBuild не имеет параметра rest; workoutBuild.ts:77-93 buildWorkoutProgram без rest — модель сама ставит rest_seconds. (Для сравнения: free-ветка workout_templates.dart:302,323 ставит kUseDefaultRest, т.е. уважает дефолт пользователя)
- **[HIGH/M] food-1** — Тап по залогированному блюду → детальный шит с просмотром/ручной правкой КБЖУ/сахара/клетчатки
  - _код:_ food_screen.dart:672-761 _FoodRow — только SwipeToDelete + кнопка X (onPressed delete), нет onTap на просмотр; в daos/food_logs_dao.dart нет метода updateLog/editLog (grep update/edit пуст)
- **[HIGH/L] kai-mascot-rive** — Живой Kai: движение/смена формы/реакции (сейчас статичный painter, не Rive)
  - _код:_ grep по '.riv'/RiveAnimation в app/lib не находит ничего (только flutter_riverpod); kai_mascot.dart — статичный CustomPainter
- **[MEDIUM/M] W1-scheme** — Выбор схемы программы (сплит/фулбоди/push-pull-legs) в опроснике и промпте
  - _код:_ ai_workout_sheet.dart:75-86 поля только goal/experience/equipment/days/minutes/focus/limitations — поля схемы нет; workoutBuild.ts:160-237 промпт не получает scheme, модель сама решает (строка 221 «names the split»)
- **[MEDIUM/S] W3-must-include** — Поле «обязательные упражнения» (must-include)
  - _код:_ ai_workout_sheet.dart:82-83 есть focus и limitations, но must-include нет; workoutBuild.ts:201-207 промпт обрабатывает focus как «emphasis», не как обязательную вставку конкретных упражнений
- **[MEDIUM/S] W6-undo-snack** — БАГ: Undo-снэкбар после удаления упражнения «висит бесконечно»
  - _код:_ undo_snack_bar.dart:62 duration=4s и :43 hideCurrentSnackBar — код корректен; workout_editor_screen.dart:101-113 _deleteExercise вызывает showUndoSnackBar один раз. Признаков MaterialBanner/бесконечной длительности нет → код не воспроизводит баг сам по себе
- **[MEDIUM/S] W7-note-formcues** — note упражнения = повтор reps+похвала вместо форм-кью
  - _код:_ workoutBuild.ts:192-193 note описан как «optional short note»; :224-232 отдельный coach-note для всей программы; инструкции писать в note именно технику выполнения (форм-кью под конкретное name) нет
- **[MEDIUM/M] W8-total-duration** — Показ общей длительности тренировки (подходы+отдых)
  - _код:_ grep по features/health не нашёл оценки длительности в workout-файлах (есть только в breathing/meditation); workout_editor_screen.dart и ai_workout_sheet.dart суммарное время дня не показывают; workout_templates.dart не содержит расчёта total
- **[MEDIUM/M] plan-5** — Drag повторяющейся задачи не вызывает диалог RecurrenceEditScope
  - _код:_ time_grid.dart:1700-1713 — явный TODO(B4): drag виртуального повтора ВСЕГДА применяет onlyThis через materializeOccurrence, без выбора области. Диалог showRecurrenceScopeDialog(RecurrenceEditScope onlyThis/thisAndFuture/wholeSeries) реализован в recurrence_scope_dialog.dart:36-143, но из drag-конвейера не вызывается. Причина в коде: _commitDrag вызывается из _DragGestureHandler без await и без BuildContext для показа диалога.
- **[MEDIUM/M] today-2-streak-nomain** — Стрик не засчитывается в день без main-задач
  - _код:_ app/lib/services/streak/streak_service.dart:47-49 — 'final mainItems = await _itemsDao.mainItemsForDay(day); if (mainItems.isEmpty) return;' — серия НЕ обновляется, если нет ни одной main. Поведение точно совпадает с описанием бага (STATUS 804-808).
- **[MEDIUM/S] today-3-anim-block** — Анимация завершения блокирует быстрое закрытие нескольких задач подряд
  - _код:_ today_screen.dart:835-855/_inFlight и task_list.dart:329-348/_inFlightActions — защита от ДВОЙНОГО свайпа одной карточки (баг дублей), не очередь завершений. Для done confirmDismiss возвращает false (today_screen.dart:800-805) → нет физической dismiss-анимации; тап по узлу хребта _doDone (строки 774, 857) выполняется мгновенно без анимации. Явной очереди завершений / снятия лока нет.
- **[MEDIUM/L] food-2** — Авто-сборка списка покупок из утверждённого ИИ-меню / меню дня
  - _код:_ ai_menu_sheet.dart: _applyAll (стр.173) пишет только в food_logs, ни одной ссылки на shopping/insertItem/ingredient (grep пуст); shopping_list_screen.dart наполняется только вручную (_submit стр.73) и из истории-предложений
- **[MEDIUM/L] food-5** — Расширить редактор рецептов: описание, пошаговые фото, видео-URL
  - _код:_ database.dart:235-252 RecipesTable содержит только id/name/createdAt/updatedAt — нет колонок description/steps/photos/videoUrl; recipe_editor_screen.dart позволяет только ингредиенты+граммы+лог, полей описания/шагов/видео нет
- **[MEDIUM/M] 1** — Дневник История: год-календарь heatmap по настроению + просмотр записи дня
  - _код:_ app/lib/features/diary/diary_history_screen.dart:88 — экран использует пошаговый DateNavigator (одна дата), а не год-вид/heatmap; _buildDayContent (строки 118-237) показывает mood-эмодзи, заметку, теги и AI-инсайт, но НЕТ кнопки «изменить» и нет окрашенного годового календаря; маршрут /diary-history существует (diary_screen.dart:270)
- **[MEDIUM/M] 3** — Профиль: редактировать имя и аватар
  - _код:_ app/lib/features/profile/profile_screen.dart:2501-2511 — ProfileAccountScreen показывает name/email только для чтения, никакого редактора; :454 _UserHeader всегда рисует дефолтную иконку PhosphorIcons.user (аватар не настраивается); profile_account_screen.dart — лишь export-стаб
- **[MEDIUM/M] today-timeline-fullday** — Today: показывать весь день целиком, а не только занятые часы
  - _код:_ today_screen.dart:617 — _TodayTimeline рендерит только строки задач (sorted) + одну now-line между ними; пустые часы дня не отрисовываются, поэтому 'сейчас' висит без контекста
- **[MEDIUM/S] water-weekly-headline-sum** — Баг: главный недельный показатель воды = СУММА, а не среднее/день
  - _код:_ health_screen.dart:77 weekWaterProvider отдаёт 7 дневных сумм; в тесте крупная цифра показала 4750 (сумма недели) при верном 'в среднем 679 мл/день' ниже
- **[MEDIUM/M] streak-history** — Прогресс: история стрика (когда держался/оборвался)
  - _код:_ grep 'streak.*history'/StreakHistory по app/lib — ничего; экрана истории стрика нет
- **[MEDIUM/M] profile-name-avatar** — Профиль: редактирование имени и аватара
  - _код:_ profile_screen.dart — нет editName/смены аватара (только displayName выводится на :1560); image_picker для аватара не подключён
- **[MEDIUM/L] adaptive-layout-audit** — Ре-аудит адаптивной вёрстки на всех размерах/ориентациях
  - _код:_ STATUS.md:900 — требование; баг переключателя Плана (plan-1) опроверг старую отметку 'адаптив готов'
- **[LOW/M] W2-intensity** — Техники интенсивности (дропсеты/суперсеты) — поле и пометка в промпте
  - _код:_ ai_workout_sheet.dart — нет поля про дропсеты/суперсеты; workoutBuild.ts:25-46 схема упражнения (name/sets/reps/rest/note) не содержит признака техники, промпт не упоминает интенсивность
- **[LOW/M] D1-3** — Per-task напоминания НЕ перепланируются при смене таймзоны (TODO LOW стр.596)
  - _код:_ refreshTimezone() реализован :139-143; reschedule() (:600-621) перепланирует ТОЛЬКО reviews+posture; TODO(notifications, LOW) на :596-599 явно фиксирует, что scheduleTaskReminder не перепланируется при смене tz
- **[LOW/S] food-4** — Блок «рекомендация» в списке покупок непонятен (на чём основан)
  - _код:_ shopping_list_screen.dart:338-420 _SuggestedSection: чипы с avatar-иконкой «+» (действие добавления очевидно) и заголовком, НО строка food.suggested_section = просто «Рекомендуется»/«Suggested for you» (food.dart:787) — источник («на основе недавней еды») не подписан
- **[LOW/M] water-charts-month** — Отчёты воды: ломаная кривая + вид 'месяц' (не только столбцы за 7 дней)
  - _код:_ health_screen.dart:937 _WeekWaterChart рисует только 7 столбцов; water_report_screen показывает дневную статистику, нет line-кривой и месячного диапазона
- **[LOW/S] rate-app-store** — 'Оценить приложение' ведёт на заглушку, а не в магазин
  - _код:_ profile_screen.dart:333-340 — onTap показывает snackbar profile.rate_coming_soon, ссылки на стор нет
- **[LOW/S] ai-photo-latency** — ИИ-фото еды: долгое ожидание перед подтверждением фото
  - _код:_ Жалоба теста #14 (🔍 investigate): после съёмки долгая пауза перед показом распознанного
- **[LOW/M] onboarding-ux-polish** — Онбординг: анимации слайдов, иллюстрации, прогресс-бар шагов
  - _код:_ STATUS.md:962 — список потенциальных улучшений онбординга после живой проверки
- **[LOW/M] internal-support-channel** — Внутренняя поддержка (расширить за рамки mailto)
  - _код:_ батч 2026-06-30 #25 — кнопка поддержки чинена на mailto support.kaname@gmail.com + fallback в буфер; полноценного канала (форма/чат) нет

## 🔒 Блокировано (аккаунты/ключи/железо) (5)

- **[HIGH/M] auth-pwreset-email** — Восстановление пароля по email не доставляется на проде (нет SMTP/email-провайдера)
  - _код:_ backend/src/routes/auth-reset.ts:62 — TODO про SMTP/SES; код только генерит и пишет код в БД (строки 44-63), на проде НЕ возвращает (devCodeAllowed, строки 19-22, 65-70). Grep по nodemailer/SMTP/SES/sendgrid/resend/mailgun в backend дал только этот TODO — пакет email-провайдера НЕ установлен.
  - _рек:_ Подключить бесплатный транзакционный email-провайдер (Resend / Brevo / Amazon SES — бесплатные лимиты есть) — разовая настройка пользователя (аккаунт+API-ключ в .env, как Gemini/Render). Затем код может вписать отправку письма в forgot-password (строка 62) и задеплоить. Логика кода/сброса/одноразовости уже готова и работает — блокирует только внешний аккаунт/ключ. Рекомендую Resend: простой API, щедрый free tier, не требует своего домена для старта.
- **[MEDIUM/S] notif-burst-device-recheck** — Залп уведомлений при старте — фикс сделан, нужна перепроверка на устройстве
  - _код:_ батч #1: убран MY_PACKAGE_REPLACED из boot-receiver (реплеил кэш при переустановке) + рефактор nextInstanceAfterNow +19 тестов; в коде поправлено, но эффект подтверждается только на железе
  - _рек:_ Blocked проверкой на Redmi (device-testing-xiaomi). Переустановить APK и убедиться, что залпа нет. До подтверждения не мержить ветку в main.
- **[LOW/S] app-icon-custom** — Кастомная иконка приложения не применена
  - _код:_ grep flutter_launcher_icons/flutter_icons по pubspec — не настроено; стоит дефолтная иконка
  - _рек:_ Blocked: нужен файл иконки от пользователя (лежит на рабочем столе). После получения — настроить flutter_launcher_icons и сгенерировать. Связано с премиум-фичей 'иконки под тему'.
- **[LOW/M] widget-ios-build** — iOS-виджет: код написан, нужна сборка/проверка на Mac
  - _код:_ app/ios/KaizenWidget/*.swift (KaizenWidget.swift, Provider.swift, KaizenEntry.swift, View) — код WidgetKit есть; сборку/проверку нельзя без macOS
  - _рек:_ Blocked железом: код готов, остаётся собрать и проверить на Mac (как и iOS-сборка вообще). Android-виджет работает. Держать до доступа к Mac.
- **[LOW/L] smartwatch-f4** — [Ф4] Умные часы: Wear OS tile + watchOS complication
  - _код:_ STATUS.md:954 — требует Apple Developer Account + физические часы
  - _рек:_ Blocked внешними аккаунтами/железом. Фаза 4, далеко за MVP — не трогать сейчас.

## 🟡 Частично сделано (15)

- **[HIGH/M] auth-pwreset-email** — Восстановление пароля по email не доставляется на проде (нет SMTP/email-провайдера)
  - _код:_ backend/src/routes/auth-reset.ts:62 — TODO про SMTP/SES; код только генерит и пишет код в БД (строки 44-63), на проде НЕ возвращает (devCodeAllowed, строки 19-22, 65-70). Grep по nodemailer/SMTP/SES/sendgrid/resend/mailgun в backend дал только этот TODO — пакет email-провайдера НЕ установлен.
- **[MEDIUM/S] W6-undo-snack** — БАГ: Undo-снэкбар после удаления упражнения «висит бесконечно»
  - _код:_ undo_snack_bar.dart:62 duration=4s и :43 hideCurrentSnackBar — код корректен; workout_editor_screen.dart:101-113 _deleteExercise вызывает showUndoSnackBar один раз. Признаков MaterialBanner/бесконечной длительности нет → код не воспроизводит баг сам по себе
- **[MEDIUM/S] D1-2** — В манифесте есть SCHEDULE_EXACT_ALARM, но НЕТ USE_EXACT_ALARM
  - _код:_ AndroidManifest.xml:9 объявлен android.permission.SCHEDULE_EXACT_ALARM; :13 RECEIVE_BOOT_COMPLETED; :4 POST_NOTIFICATIONS. USE_EXACT_ALARM в файле отсутствует
- **[MEDIUM/S] today-3-anim-block** — Анимация завершения блокирует быстрое закрытие нескольких задач подряд
  - _код:_ today_screen.dart:835-855/_inFlight и task_list.dart:329-348/_inFlightActions — защита от ДВОЙНОГО свайпа одной карточки (баг дублей), не очередь завершений. Для done confirmDismiss возвращает false (today_screen.dart:800-805) → нет физической dismiss-анимации; тап по узлу хребта _doDone (строки 774, 857) выполняется мгновенно без анимации. Явной очереди завершений / снятия лока нет.
- **[MEDIUM/M] 1** — Дневник История: год-календарь heatmap по настроению + просмотр записи дня
  - _код:_ app/lib/features/diary/diary_history_screen.dart:88 — экран использует пошаговый DateNavigator (одна дата), а не год-вид/heatmap; _buildDayContent (строки 118-237) показывает mood-эмодзи, заметку, теги и AI-инсайт, но НЕТ кнопки «изменить» и нет окрашенного годового календаря; маршрут /diary-history существует (diary_screen.dart:270)
- **[MEDIUM/M] terms-privacy-content** — Условия использования + конфиденциальность — переписать содержание
  - _код:_ terms_screen.dart существует (экран есть), но содержание сырое/шаблонное; нужно описать сбор данных, локальное хранение/синк, ИИ-данные, возраст, согласие
- **[MEDIUM/S] notif-burst-device-recheck** — Залп уведомлений при старте — фикс сделан, нужна перепроверка на устройстве
  - _код:_ батч #1: убран MY_PACKAGE_REPLACED из boot-receiver (реплеил кэш при переустановке) + рефактор nextInstanceAfterNow +19 тестов; в коде поправлено, но эффект подтверждается только на железе
- **[LOW/S] today-6-completion-sound** — Звук завершения задачи — поменять
  - _код:_ Система звука есть и подключена: completion_sound_service.dart (играет assets/sounds/task_done.wav, fallback SystemSound.click), хук в items_dao.dart:210,474,544 (markDone/materializeOccurrence), тумблер в profile_screen.dart:1321-1335. Ассет app/assets/sounds/task_done.wav СУЩЕСТВУЕТ (синт. колокол E6→B6, ~0.6с, см. комментарий completion_sound_service.dart:35-37). README.md устарел (всё ещё пишет 'task_done.mp3 — TODO').
- **[LOW/S] food-4** — Блок «рекомендация» в списке покупок непонятен (на чём основан)
  - _код:_ shopping_list_screen.dart:338-420 _SuggestedSection: чипы с avatar-иконкой «+» (действие добавления очевидно) и заголовком, НО строка food.suggested_section = просто «Рекомендуется»/«Suggested for you» (food.dart:787) — источник («на основе недавней еды») не подписан
- **[LOW/S] 2** — Дневник: состояние после сохранения / редактирование записи без дублей
  - _код:_ app/lib/features/diary/diary_screen.dart:61 _loadExisting() в initState читает запись за сегодня и заполняет форму (_mood, _parseNote) → форма НЕ пустая при повторном открытии; :102 dao.saveForDate — upsert (один ряд на день); :111-124 mood_logs first-save-wins (нет дублей); :130 после сохранения только SnackBar 'diary.day_saved'
- **[LOW/S] health-grouping** — Health — 4 группы (Питание/Осознанность/Движение/Сон) + убрать дефолтные вода+сон
  - _код:_ ГРУППИРОВКА СДЕЛАНА: health_screen.dart 4 секции — section_nutrition (Вода card стр.228 + Food tile стр.230), section_sleep (стр.240, _SleepCard), section_mind (Медитация стр.245 + Дыхание стр.254), section_movement (Workouts стр.265). Но «убрать дефолтные вода+сон» НЕ сделано: Вода (стр.228) и Сон (стр.241) по-прежнему всегда видимы, не за тумблерами/онбордингом. Движение содержит только Workouts — Разминка/Осанка отдельными тайлами в хабе не выведены (Posture = reminder-only по app/CLAUDE.md).
- **[LOW/M] water-charts-month** — Отчёты воды: ломаная кривая + вид 'месяц' (не только столбцы за 7 дней)
  - _код:_ health_screen.dart:937 _WeekWaterChart рисует только 7 столбцов; water_report_screen показывает дневную статистику, нет line-кривой и месячного диапазона
- **[LOW/S] diary-save-affordance** — Дневник: неочевидное состояние после сохранения дня
  - _код:_ diary_screen.dart:61-66 _loadExisting() уже подтягивает запись дня (prefill mood/note), дублей нет; не хватает явного UI 'сегодня записано: … [изменить]'
- **[LOW/M] widget-ios-build** — iOS-виджет: код написан, нужна сборка/проверка на Mac
  - _код:_ app/ios/KaizenWidget/*.swift (KaizenWidget.swift, Provider.swift, KaizenEntry.swift, View) — код WidgetKit есть; сборку/проверку нельзя без macOS
- **[LOW/M] internal-support-channel** — Внутренняя поддержка (расширить за рамки mailto)
  - _код:_ батч 2026-06-30 #25 — кнопка поддержки чинена на mailto support.kaname@gmail.com + fallback в буфер; полноценного канала (форма/чат) нет

## ✅ Оказалось уже готово (22)

- **[HIGH/S] ai-morning-redistribute** — Morning-message даёт умный фокус-совет, redistribute учитывает priority/type/time
  - _код:_ backend/src/ai/morningMessage.ts:33-52 — промпт запрещает пустые ободрялки ('you can do it','don't worry'), запрещает пересказ числа задач, требует ONE concrete focus tip; maxTokens 180 (было 120). backend/src/ai/smartRedistribute.ts:73-92 — промпт: main>high>medium>low (priority), 30-мин слоты/занятые времена (time), task-by-task reason по title+времени; maxTokens 1500.
- **[HIGH/S] ai-diary-insight** — Diary-insight: лимит токенов поднят, промпт против пересказа, роут шлёт 7 дней
  - _код:_ backend/src/ai/diaryInsight.ts:88-94 — maxTokens 650 (был 450); строки 64-75 — промпт требует НЕочевидный инсайт/тренд/корреляцию, запрещает пересказ записей, требует завершать предложения ('Complete every sentence fully'). Роут backend/src/routes/ai.ts:586-591 — prisma.dayLog.findMany take:7 (шлёт 7 дней, не 1), диапазон дат пробрасывается в covered_from/covered_to (ai.ts:605-606).
- **[HIGH/S] D1-1** — Критичные уведомления используют exactAllowWhileIdle (с откатом на inexact)
  - _код:_ notification_service.dart:40-44 resolveScheduleMode (null/true→exactAllowWhileIdle, false→inexact); :204-214 _chooseScheduleMode вызывает canScheduleExactNotifications(); все 8 zonedSchedule (:293,302 reviews; :351 posture; :422 task; :484 habit) используют mode=await _chooseScheduleMode(), хардкода inexact больше нет
- **[HIGH/S] D1-4** — scheduleTaskReminder полностью реализована (не заглушка)
  - _код:_ notification_service.dart:406-430: стабильный id taskReminderId/FNV-1a (:391-399), отмена прежнего, проверка fireAt в прошлом (:419), zonedSchedule с _chooseScheduleMode и каналом kaizen_tasks (importance.high); cancelTaskReminder :433-437
- **[HIGH/S] plan-1** — Переключатель промежутка ломается — подписи переносятся вертикально по буквам
  - _код:_ plan_screen.dart:577-672 — _PlanViewSwitcher адаптивен: измеряет ширину (_segmentsFit, _measure через TextPainter) и при нехватке места переходит в компактный _ViewDropdown; все подписи single-line (maxLines:1, softWrap:false, overflow:ellipsis), строки 635-657. На mobile тулбар использует _ViewDropdown напрямую (строка 360). Вертикальный перенос по буквам исключён.
- **[HIGH/S] plan-2** — Overflow 27px на 6-недельном месяце + пустой день (импорт)
  - _код:_ plan_screen.dart:500-520 (Day) и 476-498 (Week): высота раскрытого календаря зажата maxCalH = 55% высоты тела (clamp 220..). expandable_week_calendar.dart:232-246 — effectiveGridH вычитает подписи/граббер/шапку, не даёт Column переполниться (явно 'Bug #2 fix'). Пустое состояние day_timeline.dart:123-168 обёрнуто в LayoutBuilder+SingleChildScrollView+ConstrainedBox(minHeight) — блок импорта скроллится, а не переполняет.
- **[HIGH/S] plan-3** — Plan (веб) — пустые серые плашки вместо контента/лоадера
  - _код:_ Loading обработан во всех видах Plan: day_timeline.dart:43-44 (isLoading && valueOrNull==null → KaiLoader), month_view.dart:74-75, week_agenda.dart:150 + LinearProgressIndicator:205. Серые плашки = необработанный loading первого emit Drift — теперь показывается KaiLoader вместо пустых ячеек.
- **[HIGH/S] today-4-undo-bug** — БАГ HIGH: Undo после выполнения задачи не возвращает её (обычные и повторяющиеся)
  - _код:_ Обычная: today_screen.dart:896-904 и task_list.dart:395-405 — onUndo делает updateItem(status:pending, updatedAt:now). Повторяющаяся: today_screen.dart:890-895 / task_list.dart:387-394 → undoMaterializeOccurrence(anchorId,date,concreteId), реализован в items_dao.dart:590-611 (удаляет concrete-строку + снимает EXDATE). Кнопка Undo вызывает onUndo: app_toast.dart:258-261. Тест test/today_undo_test.dart покрывает обычную + виртуальную + локализацию.
- **[MEDIUM/S] ai-wrapped-summary** — Wrapped-summary даёт инсайт/совет вместо пересказа цифр
  - _код:_ backend/src/ai/wrappedSummary.ts:37-42 — промпт явно 'DO NOT list or repeat the numbers', требует ONE strength + ONE growth area + ONE next-week recommendation; maxTokens 350 (был 120, убран лимит '60 слов'). Строки 45-67 — числа передаются как контекст для вывода, юзеру шлётся 'Do not list these numbers back'.
- **[MEDIUM/S] D1-overall** — Итог: D1 в STATUS помечен done — подтверждается кодом, с двумя оговорками
  - _код:_ Манифест: receivers :55-76, разрешения :9,13. Service: exact-режим, rescheduleAllReminders :252-269. Расхождение: STATUS.md:9 утверждает intent-filter включает MY_PACKAGE_REPLACED, но в манифесте :64-72 его НАМЕРЕННО нет (комментарий :58-63 объясняет: избегают залпа уведомлений при обновлении)
- **[MEDIUM/S] today-1-header** — Шапка Today перегружена (приветствие+Kai+стрик), задачи уезжают вниз
  - _код:_ app/lib/features/today/today_screen.dart:8-11 (комментарий 'УБРАНО: ProgressRing, StreakRow, большой KaiMascot, тумблер тона'); _QuietHeader строки 225-264 = только дата + одна строка приветствия, без аватара/шестерёнки/стрика; Kai вынесен в тонкую строку _KaiReviewRow (22dp), показывается лишь при просрочке/разборе (строки 100-101, 324-331).
- **[LOW/S] today-5-undo-hardcode** — Текст кнопки Undo захардкожен 'Undo'
  - _код:_ app/lib/core/animations/app_toast.dart:262 — 'child: Text(context.s('common.undo'))'. Хардкода нет; ключ common.undo есть. Файл всего 273 строки — ссылка STATUS на 'app_toast.dart:274 Undo' (строка 822) устарела и не соответствует коду.
- **[LOW/S] food-3** — Кнопка «убрать отмеченные» в списке покупок незаметна
  - _код:_ shopping_list_screen.dart:171-178 полноширинный _ClearCheckedBanner (FilledButton.tonal с «Убрать купленные (N)») над списком при hasChecked + дублирующий AppBar action (стр.117-123) + Dismissible свайп-удаление (стр.273)
- **[LOW/S] food-7** — Хардкод англ. строк в food_screen ('pick a match', '$kcal kcal / 100g')
  - _код:_ grep 'pick a match'/'kcal / 100' по app/lib/features/food = 0 совпадений; food_screen.dart:1189-1191 использует context.s('food.kcal_per_100g'), стр.698-705 row_grams_kcal, anti-regress grep по hardcoded Text в food_screen чист
- **[LOW/S] 4** — Профиль: внутренняя поддержка (расширить Send feedback)
  - _код:_ app/lib/features/profile/profile_screen.dart:177 _launchSupportEmail() открывает mailto:support.kaname@gmail.com с темой, при отсутствии клиента — копирует адрес в буфер + SnackBar 'profile.support_copied'; вызывается из строки «Send feedback» (:343-348)
- **[LOW/S] 5** — «Оценить приложение» → магазин (стаб)
  - _код:_ app/lib/features/profile/profile_screen.dart:334-340 — кнопка rate_app показывает SnackBar 'profile.rate_coming_soon' («скоро в магазинах») — это ровно запрошенная заглушка
- **[LOW/S] 6** — Маркер версии/сборки в Профиле (ветка+commit+дата)
  - _код:_ app/lib/core/utils/app_version.dart:4 kAppBuildTag = String.fromEnvironment('APP_BUILD_TAG'); profile_screen.dart:2306-2314 _AppVersionLabel выводит 'v{version} (build {buildNumber} · {tag}) · debug' с тегом сборки и пометкой debug
- **[LOW/S] 7** — Dev-разблокировка премиума
  - _код:_ app/lib/services/purchases/purchase_service.dart:65-72 StubPurchaseService.buyPremium в kDebugMode зовёт _apiClient.devUpgrade(tier:'premium'); paywall_screen.dart:375-392 блок Dev tools (только kDebugMode) с кнопками «Activate Premium (dev only)» / «Downgrade to Free»; isPremiumProvider инвалидируется после
- **[LOW/S] kai-tone-settings** — Kai+тон — свести три пересекающиеся настройки (тон/личность/частота) к 1-2
  - _код:_ В коде НЕ три, а ДВЕ настройки: profile_screen.dart:1917 _KaiSettingsSection содержит только _ShowKaiSetting (showKaiProvider, mascot_provider.dart) + _ToneRow (toneProvider gentle/harsh, tone_provider.dart:11). Grep personality|frequency|kai_personality|nudge_frequency по app/lib — совпадений нет (личность Kai и частота подсказок как настройки в коде ОТСУТСТВУЮТ). Описанная в STATUS.md:896-899 проблема «трёх кнопок» не отражает текущий код.
- **[LOW/S] main-task-shield** — Щит (protected-индикатор) на главной задаче — нужен ли
  - _код:_ Щит реализован и отображается: today_screen.dart:1190 PhosphorIcons.shield(PhosphorIconsStyle.fill) для main-задач; поле database.dart:49 isProtected = boolean().withDefault(false). Функция «protected = перенос не двигает» жива в коде.
- **[LOW/S] version-marker** — Маркер версии/сборки в Профиле (для тестов)
  - _код:_ profile_screen.dart:2294-2312 _AppVersionLabel выводит 'v{version} (build {buildNumber}{tag}{debug})' через PackageInfo + app_version.dart
- **[LOW/S] dev-premium-unlock** — Дев-разблокировка премиума для теста ИИ
  - _код:_ backend/src/routes/subscription.ts:21-32 — роут dev-upgrade с гейтом ALLOW_DEV_UPGRADE (разрешён на Render для теста, заблокирован в prod без флага)

